⚙️ Локально:
    Приоритет у менеджера - json, если данные в .db не совпадают с .json, то они будут перезаписанны на данные из .json
☁️ Через сервер:
    Приоритет у менеджера - сервер, если данные на сервере не совпадают с локальными, тогда локальные будут перезаписанны на данные с сервера
